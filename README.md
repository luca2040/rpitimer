### rpitimer

_Raspberry pi zero_ based server to turn on and off devices with timers (or manually) set from web interface. <br/>
Pinout can be configured via the `devices.json` and `timers.json` files.

The included `rpitimer.service.example` file can be used as an example to set up the app as a systemd startup service.
