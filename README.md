### rpitimer

_Raspberry pi zero_ based server to turn on and off devices with timers (or manually) set from web interface. <br/>
Pinout can be configured via the `devices.json` file.

The included `configure.sh` script can be used to set the app as a startup service.
